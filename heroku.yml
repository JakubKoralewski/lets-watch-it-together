setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
    - plan: heroku-redis:hobby-dev
      as: REDIS

build:
  docker:
    web: Dockerfile
    worker: prisma/Dockerfile.worker

release:
  image: web
  command:
    - ./prisma/heroku-release.sh
    